<Window x:Class="DND.Creation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DND"
        mc:Ignorable="d"
        Title="Creation" Name="CreationName" Height="800" Width="1280" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <Frame Name="Frame1"/>
        <DockPanel Grid.Row="1">
            <DockPanel.Background>
                <ImageBrush ImageSource="Resources/CreatorMain/Banner1.jpg" Stretch="UniformToFill"/>
            </DockPanel.Background>
            <Button Name="Btn_Continue" Margin="20" Padding="5" HorizontalAlignment="Right" VerticalAlignment="Center" Click="Btn_Continue_Click" Content="Continue"/>
        </DockPanel>
    </Grid>
</Window>
